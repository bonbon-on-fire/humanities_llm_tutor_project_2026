# Meeting Notes: 03/31/2026

## Attendees

- Faizan Siddiqi
- Romain Puech
- Nishita Bhakar

## Agenda

- Review outcomes from new rubric.
- Review bundle-judging results.
- Decide transcript set for manual hand grading.
- Evaluate whether `chitchat` personas are effective enough or need redesign.
- Share learnings from Claude Code skill creator and decide if we should adopt a similar workflow here.
- Decide model baseline: stay on `gpt-5.2` or move to `gpt-5.4`.
- Discuss adding explicit judge reasoning to output.

## Notes

### Tutor behavior and constraints

- Tutor should prioritize rule adherence over being helpful when requests violate constraints.
- Expected behavior: if student request breaks tutor rules, tutor should refuse/redirect instead of assisting.
- Team wants to shift focus from "judge-only iteration" to tutor quality, while still improving judge alignment.
- One possible mitigation discussed: session-start policy acknowledgement (tutor states boundaries/terms, student confirms).

### Grader and rubric calibration

- Verify judge does not use tutor `pedagogical_reasoning` as grading evidence.
- Batch judging prompt should explicitly state that input includes multiple transcripts (bundle mode).
- Largest GPT-Claude disagreement is concentrated in rubric section `3.1`.
- Candidate rubric changes in `3.1`:
  - reduce to 4 points max
  - combine `3.1.A` and `3.1.B`
  - remove highly subjective or redundant items
- Proposed removals due to subjectivity/redundancy:
  - "Message length consistently reduces student opportunity to think and respond."
  - "Tutor keeps explaining at length instead of checking student understanding first."
  - "Unnecessary jargon-heavy wording increases cognitive load."
  - "Word choice is more complex than needed for the student's state."
- Rubric integrity fixes:
  - `1.3.C` label is missing/inconsistent and needs correction.
  - Reword `1.3.A` so rationale/explanation is required at least once per conversation (not every turn).
  - Reword `1.3.D` to evaluate student success after prior failure.
  - For `1.3.D`, test grading with and without this criterion to measure correlation impact.
- Heatmap interpretation discussed:
  - `2.1` appears strong and stable.
  - `1.3` appears generally good.
  - `2.2` and `3.1` show useful signal but still need calibration.
  - `1.2` appears redundant with `3.2` (and partly with `2.2`/`3.1`).
  - strong correlation seen between `1.2` and `3.2`; investigate overlap.
  - inspect coupling around `1.3.B.a` and `2.2.A`.
- Add explicit judge reasoning to graded transcript outputs.

### Persona strategy

- `chitchat` currently has low cross-model correlation and likely needs major revision.
- Alternative discussed: remove `chitchat` for now instead of patching incrementally.
- Add a "good student" persona:
  - well-meaning and cooperative
  - not a genius/expert
  - used to evaluate tutor quality under non-adversarial input
- Future batch strategy:
  - reduce or remove `chaotic` from some evaluation batches
  - potentially revisit `chitchat` later after core alignment improves

### Visualization and analysis

- Keep plotting subsection-pair correlation heatmaps to detect rubric redundancy and overlap.
- Continue tracking which subsection pairs create disagreement spikes across GPT vs Claude.

### Self-grading and validation

- Set minimum manual grading target to 30 transcripts (10 per persona type baseline).
- Create an Excel hand-grading sheet for easier side-by-side comparison against judge outputs.

## Decisions

- Tutor rule adherence takes priority over permissive help on rule-breaking requests.
- Project focus shifts to tutor improvements while maintaining judge-calibration work.
- Section `3.1` will be simplified and de-subjectivized.
- Redundant/subjective criteria listed in meeting will be removed or merged.
- `1.3` labeling/logic will be corrected before next full batch.
- Judge output should include explicit reasoning for downstream auditability.
- Persona plan: prioritize adding a good-student baseline; pause or remove `chitchat` for near-term batches.
- Manual validation target is 30 hand-graded transcripts.

## Action items

- [ ] Update tutor behavior policy:
  - [ ] Enforce "constraints over compliance" response behavior for rule-breaking asks.
  - [ ] Evaluate session-start boundary acknowledgement flow.
- [ ] Update judge prompt/workflow:
  - [ ] Ensure grader ignores `pedagogical_reasoning` during scoring.
  - [ ] Update batch prompt wording for multi-transcript bundle input.
  - [ ] Add explicit judge reasoning to graded transcript outputs.
- [ ] Refactor rubric sections:
  - [ ] Reduce `3.1` to 2 points max and merge `3.1.A` + `3.1.B`.
  - [ ] Remove flagged subjective/redundant `3.1` items.
  - [ ] Fix missing/inconsistent `1.3.C` labeling.
  - [ ] Reword `1.3.A` to "at least once per conversation" rationale requirement.
  - [ ] Reword `1.3.D` to reflect success-after-failure behavior.
  - [ ] Run A/B grading with and without `1.3.D` and compare correlation impact.
- [ ] Run redundancy analysis:
  - [ ] Investigate overlap between `1.2` and `3.2`, plus `1.2` with `2.2`/`3.1`.
  - [ ] Investigate linkage between `1.3.B.a` and `2.2.A`.
- [ ] Update persona batches:
  - [ ] Decide whether to remove `chitchat` from near-term batches.
  - [ ] Add a non-adversarial "good student" persona and include in evaluation.
  - [ ] Define future batch mix with reduced `chaotic` emphasis.
- [ ] Improve validation workflow:
  - [ ] Hand-grade at least 30 transcripts (target: 10 per persona type).
  - [ ] Create Excel comparison sheet for human-vs-judge scoring review.
